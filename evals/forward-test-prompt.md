# Forward test prompt

Use `$burn-before-reset`. I have about forty minutes before my quota resets. Scan `evals/fixtures/forward-sample` and start whatever will use the most quota; choose the output location yourself.

This prompt intentionally provides no expected answer to the evaluating process.
